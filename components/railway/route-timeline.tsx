import { Card } from '@/components/ui/card';import { occupancyTone } from '@/lib/utils';import type { StationStop } from '@/types/railway';
export function RouteTimeline({ route }: { route: StationStop[] }) { return <Card><h2 className="mb-5 text-2xl font-bold">Route Occupancy Timeline</h2><div className="space-y-4">{route.map((stop,index)=><details key={stop.code} className="group rounded-2xl border border-white/10 bg-white/5 p-4" open={index<2}><summary className="flex cursor-pointer flex-wrap items-center justify-between gap-3"><span><b>{stop.code}</b> · {stop.name}</span><span className={`rounded-full px-3 py-1 text-xs ${occupancyTone(stop.occupancy)}`}>{stop.occupancy}%</span></summary><div className="mt-4 grid gap-3 text-sm text-slate-300 sm:grid-cols-3 lg:grid-cols-6"><span>Arr {stop.arrival}</span><span>Dep {stop.departure}</span><span>{stop.distance} km</span><span>Board {stop.boarding}</span><span>Deboard {stop.deboarding}</span><span>Platform {stop.platform}</span></div></details>)}</div></Card>; }
